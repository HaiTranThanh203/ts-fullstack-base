name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  test:
    name: Install & Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: server/package-lock.json  # ← FIX 1

      - name: Install dependencies
        run: npm install
        working-directory: server  # ← FIX 2

      - name: Run tests
        run: npm run test
        working-directory: server  # ← FIX 3

  deploy:
    name: Deploy to VPS
    runs-on: ubuntu-latest
    needs: test

    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USERNAME }}
          key: ${{ secrets.SERVER_SSH_KEY }}

          script: |
            APP_DIR="/app/nestjs-crud"

            if [ -d "$APP_DIR/.git" ]; then
              echo ">>> Pulling latest code..."
              cd $APP_DIR
              git pull origin main
            else
              echo ">>> Cloning repo..."
              mkdir -p $APP_DIR
              git clone https://github.com/HaiTranThanh203/ts_fullstack_base.git $APP_DIR
              cd $APP_DIR
            fi

            # ← FIX 4: vào đúng thư mục server mới có Dockerfile
            cd $APP_DIR/server

            echo ">>> Creating .env file..."
            cat > .env << EOF
            PORT=3000
            MONGO_URI=${{ secrets.MONGO_URI_PROD }}
            EOF

            echo ">>> Building and starting Docker containers..."
            docker-compose up -d --build

            echo ">>> Cleaning up old images..."
            docker image prune -f

            echo ">>> Deployment complete!"